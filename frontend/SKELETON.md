# Frontend — Code Skeleton
<!-- AUTO-GENERATED by scripts/portkit_skeletonize.py on 2026-05-18 -->
<!-- DO NOT EDIT MANUALLY — run the skeletonizer to update -->

This file provides a structural map of the Frontend module for AI coding agents.
It contains class/function signatures, type definitions, and docstrings — **no implementation bodies**.

**Files indexed:** 60

---

### `frontend/src/components/ConversionHistory/types.ts`

**interface ConversionHistoryItem** `{ job_id: string; original_filename: string; status: 'completed' | 'failed' | 'processing' | 'queued' | 'partial'; created_at: string; completed_at?: st }`

**interface ConversionHistoryItemFromAPI** `{ conversion_id: string; status: string; progress: number; message: string; created_at: string; updated_at?: string; original_filename?: string; error?: }`

---

### `frontend/src/components/AdvancedOptions/AdvancedOptionsPanel.tsx`

**interface AdvancedOptions** `{ timeout: number; parallelProcessing: boolean; maxRetries: number; enableSmartAssumptions: boolean; enableTextureOptimization: boolean; targetVersion:  }`

**interface AdvancedOptionsPanelProps** `{ options: AdvancedOptions; onChange: (options: AdvancedOptions) => void }`

**component AdvancedOptionsPanel** (props: AdvancedOptionsPanelProps)

---

### `frontend/src/components/BatchConversion/BatchConversionManager.tsx`

**interface BatchConversionItem** `{ id: string; filename: string; file: File; status: 'pending' | 'uploading' | 'converting' | 'completed' | 'failed'; progress: number; jobId?: string; e }`

**interface BatchConversionManagerProps** `{ onComplete?: (; jobIds: string[]; results?: { succeeded: number; failed: number }`

**component BatchConversionManager** (props: BatchConversionManagerProps)

---

### `frontend/src/components/BehaviorEditor/BehaviorEditor.tsx`

**interface BehaviorEditorProps** `{ conversionId: string; className?: string }`

**interface SelectedFile** `{ id: string; path: string; type: string }`

**component BehaviorEditor** (props: BehaviorEditorProps)

---

### `frontend/src/components/BehaviorEditor/BehaviorFileTree.tsx`

**interface BehaviorFileTreeNode** `{ id: string; name: string; path: string; type: 'file' | 'directory'; file_type: string; children: BehaviorFileTreeNode[] }`

**interface BehaviorFileTreeProps** `{ conversionId: string; onFileSelect: (fileId: string; filePath: string; fileType: string) => void; selectedFileId?: string }`

**component BehaviorFileTree** (props: BehaviorFileTreeProps)

---

### `frontend/src/components/BehaviorEditor/BehaviorFileTreeEnhanced.tsx`

**interface BehaviorFileTreeNode** `{ id: string; name: string; path: string; type: 'file' | 'directory'; file_type: string; children: BehaviorFileTreeNode[] }`

**interface BehaviorFileTreeEnhancedProps** `{ conversionId: string; onFileSelect: (fileId: string; filePath: string; fileType: string) => void; selectedFileId?: string; readOnly?: boolean }`

**component BehaviorFileTreeEnhanced** (props: BehaviorFileTreeEnhancedProps)

---

### `frontend/src/components/BehaviorEditor/BlockEditor/BlockPreview.tsx`

**interface Preview3DProps** `{ properties: BlockProperties; onPropertyChange: (fieldId: string; value: any) => void; readOnly?: boolean }`

**component Preview3D** (props: Preview3DProps)

---

### `frontend/src/components/BehaviorEditor/BlockEditor/BlockPropertyEditor.tsx`

**interface BlockProperties** `{ identifier: string; displayName: string; description: string; category: string; hardness: number; resistance: number; friction: number; lightEmission: }`

**interface BlockPropertyEditorProps** `{ blockData?: Partial<BlockProperties>; onPropertiesChange: (properties: BlockProperties) => void; onValidationChange?: (errors: ValidationRule[]) => vo }`

**component BlockPropertyEditor** (props: BlockPropertyEditorProps)

---

### `frontend/src/components/BehaviorEditor/CodeEditor.tsx`

**interface BehaviorFile** `{ id: string; conversion_id: string; file_path: string; file_type: string; content: string; created_at: string; updated_at: string }`

**interface CodeEditorProps** `{ fileId: string | null; filePath: string; fileType: string; onContentChange?: (content: string) => void; onSave?: (fileId: string; content: string) =>  }`

**component CodeEditor** (props: CodeEditorProps)

---

### `frontend/src/components/BehaviorEditor/CodeEditorEnhanced.tsx`

**interface BehaviorFile** `{ id: string; conversion_id: string; file_path: string; file_type: string; content: string; created_at: string; updated_at: string }`

**interface CodeEditorEnhancedProps** `{ fileId: string | null; filePath: string; fileType: string; onContentChange?: (content: string) => void; onSave?: (fileId: string; content: string) =>  }`

**component CodeEditorEnhanced** (props: CodeEditorEnhancedProps)

---

### `frontend/src/components/BehaviorEditor/LogicBuilder/LogicBuilder.tsx`

**interface LogicNode** `{ id: string; type: 'trigger' | 'condition' | 'action' | 'variable' | 'function'; name: string; category: string; position: { x: number; y: number }`

**interface NodePort** `{ id: string; name: string; type: 'flow' | 'data'; dataType?: 'string' | 'number' | 'boolean' | 'object' | 'any'; required?: boolean; multiple?: boolean }`

**interface NodeConnection** `{ id: string; sourceNodeId: string; sourcePortId: string; targetNodeId: string; targetPortId: string }`

**interface NodeConfig** `{ description?: string; icon?: React.ReactNode; color?: string; template?: Record<string }`

**interface LogicFlow** `{ id: string; name: string; description: string; nodes: LogicNode[]; connections: NodeConnection[]; variables: LogicVariable[]; triggers: string[] }`

**interface LogicVariable** `{ id: string; name: string; type: 'string' | 'number' | 'boolean' | 'object'; defaultValue: any; scope: 'local' | 'global'; description?: string }`

**interface LogicBuilderProps** `{ flow?: LogicFlow; onFlowChange?: (flow: LogicFlow) => void; onSave?: (flow: LogicFlow) => void; onTest?: (flow: LogicFlow) => void; readOnly?: boolean }`

**component LogicBuilder** (props: LogicBuilderProps)

---

### `frontend/src/components/BehaviorEditor/LootTableEditor/LootTableEditor.tsx`

**interface LootPool** `{ id: string; name: string; conditions: LootCondition[]; functions: LootFunction[]; entries: LootEntry[]; rolls: {; min?: number; max?: number }`

**interface LootEntry** `{ id: string; type: 'item' | 'tag' | 'loot_table' | 'empty'; name: string; weight: number; quality?: number; conditions: LootCondition[]; functions: Loo }`

**interface LootCondition** `{ id: string; condition: string; parameters: Record<string }`

**interface LootFunction** `{ id: string; function: string; parameters: Record<string }`

**interface LootTable** `{ id: string; type: string; pools: LootPool[]; functions?: LootFunction[]; conditions?: LootCondition[] }`

**interface LootTableEditorProps** `{ lootTable?: LootTable; onLootTableChange?: (lootTable: LootTable) => void; onPreview?: (lootTable: LootTable) => void; onSave?: (lootTable: LootTable) }`

**component LootTableEditor** (props: LootTableEditorProps)

**component Label**

---

### `frontend/src/components/BehaviorEditor/RecipeBuilder/ItemLibrary.tsx`

**interface ItemLibraryProps** `{ items: RecipeItem[]; selectedItem: RecipeItem | null; onItemSelect: (item: RecipeItem | null) => void; onResultSelect?: (item: RecipeItem) => void; re }`

**component ItemLibrary** (props: ItemLibraryProps)

---

### `frontend/src/components/BehaviorEditor/RecipeBuilder/RecipeBuilder.tsx`

**interface RecipeItem** `{ id: string; type: | 'minecraft:item'; minecraft: item_tag'; minecraft: block'; minecraft: block_tag'; name: string; count?: number; data?: number }`

**interface RecipeSlot** `{ x: number; y: number; item: RecipeItem | null }`

**interface Recipe** `{ id: string; identifier: string; type: | 'shaped'; name: string; description?: string; pattern?: RecipeSlot[][]; ingredients: RecipeItem[]; result: Rec }`

**interface RecipeBuilderProps** `{ initialRecipe?: Partial<Recipe>; onRecipeChange: (recipe: Recipe) => void; onRecipeSave?: (recipe: Recipe) => void; availableItems: RecipeItem[]; read }`

**component RecipeBuilder** (props: RecipeBuilderProps)

---

### `frontend/src/components/BehaviorEditor/RecipeBuilder/RecipeGrid.tsx`

**interface RecipeGridProps** `{ pattern: RecipeSlot[][]; width: number; height: number; onItemPlace: (slot: RecipeSlot; item: RecipeItem) => void; onItemRemove: (slot: RecipeSlot) => }`

**component RecipeGrid** (props: RecipeGridProps)

---

### `frontend/src/components/BehaviorEditor/TemplateSelector/TemplateSelector.tsx`

**interface TemplateSelectorProps** `{ onTemplateSelect: (template: BehaviorTemplate) => void; onTemplateApply?: (template: BehaviorTemplate) => void; category?: string; templateType?: stri }`

**component TemplateSelector** (props: TemplateSelectorProps)

---

### `frontend/src/components/BehaviorEditor/VisualEditor/FormBuilder.tsx`

**interface FormBuilderProps** `{ fields: FormField[]; data: Record<string; onChange: (fieldId: string; value: any) => void; getFieldErrors: (; fieldName: string; message: string; seve }`

**component BaseTextField**

**component FormBuilder** (props: FormBuilderProps)

---

### `frontend/src/components/BehaviorEditor/VisualEditor/VisualEditor.tsx`

**interface FormField** `{ id: string; name: string; type: | 'text'; label: string; value: any; required?: boolean; min?: number; max?: number }`

**interface ValidationRule** `{ field: string; rule: string; message: string; severity: 'error' | 'warning' | 'info' }`

**interface VisualEditorProps** `{ title: string; fields: FormField[]; initialData?: Record<string; onFieldChange?: (fieldId: string; value: any) => void; onValidationChange?: (errors:  }`

**component VisualEditor** (props: VisualEditorProps)

---

### `frontend/src/components/BehavioralTest/BehavioralTest.tsx`

**interface TestScenario** `{ scenario: string; steps: Array<Record<string; expected_outcome?: string; timeout_ms?: number; fail_fast?: boolean }`

**interface BehavioralTestProps** `{ conversionId: string; onTestComplete?: (results: any) => void }`

**interface TestResult** `{ test_id: string; status: string; total_scenarios: number; passed_scenarios: number; failed_scenarios: number; behavioral_score?: number; execution_tim }`

**component PlayCircle**

**component Download**

**component RefreshCw**

**component BehavioralTest** (props: BehavioralTestProps)

---

### `frontend/src/components/BehavioralTest/BehavioralTestWrapper.tsx`

**component BehavioralTestWrapper**

---

### `frontend/src/components/ComparisonView/AssumptionTracker.tsx`

**interface SmartAssumption** `{ id: string; description: string; impact?: string; confidence?: string }`

**interface AssumptionTrackerProps** `{ assumptions: SmartAssumption[] }`

**component AssumptionTracker** (props: AssumptionTrackerProps)

---

### `frontend/src/components/ComparisonView/ComparisonReportPage.tsx`

**interface ComparisonReportPageProps** `{ fullReport: any }`

**component ComparisonReportPage** (props: ComparisonReportPageProps)

---

### `frontend/src/components/ComparisonView/ComparisonView.tsx`

**interface FeatureMappingData** `{ id: string; java_feature: string; bedrock_equivalent: string; mapping_type: string; confidence_score: number | null }`

**interface ComparisonData** `{ id: string; conversion_id: string; structural_diff: {; files_added: string[]; files_removed: string[]; files_modified: string[] }`

**component ComparisonView**

---

### `frontend/src/components/ComparisonView/DiffViewer.tsx`

**interface DiffViewerProps** `{ codeDiff: any }`

**component DiffViewer** (props: DiffViewerProps)

---

### `frontend/src/components/ComparisonView/FeatureMapper.tsx`

**interface FeatureMappingData** `{ id: string; java_feature: string; bedrock_equivalent: string; mapping_type: string; confidence_score: number | null }`

**interface FeatureMapperProps** `{ features: FeatureMappingData[] }`

**component FeatureMapper** (props: FeatureMapperProps)

---

### `frontend/src/components/ConversionAssets/ConversionAssetDetails.tsx`

**interface ConversionAssetDetailsProps** `{ asset: ConversionAsset; onAssetUpdated?: (asset: ConversionAsset) => void; onClose?: () => void }`

**component ConversionAssetDetails** (props: ConversionAssetDetailsProps)

---

### `frontend/src/components/ConversionAssets/ConversionAssetsList.tsx`

**interface ConversionAssetsListProps** `{ conversionId: string; onAssetSelect?: (asset: ConversionAsset) => void; onRefresh?: () => void }`

**component ConversionAssetsList** (props: ConversionAssetsListProps)

---

### `frontend/src/components/ConversionAssets/ConversionAssetsManager.tsx`

**interface ConversionAssetsManagerProps** `{ conversionId: string; className?: string }`

**component ConversionAssetsManager** (props: ConversionAssetsManagerProps)

---

### `frontend/src/components/ConversionAssets/ConversionAssetsUpload.tsx`

**interface ConversionAssetsUploadProps** `{ conversionId: string; onAssetUploaded?: (asset: ConversionAsset) => void }`

**component ConversionAssetsUpload** (props: ConversionAssetsUploadProps)

---

### `frontend/src/components/ConversionDashboard/ConversionDashboard.tsx`

**component ConversionDashboard**

---

### `frontend/src/components/ConversionFlow/ConversionFlowManager.tsx`

**interface ConversionFlowState** `{ jobId: string | null; filename: string; status: | 'idle'; progress: number; error: string | null; resultUrl: string | null; friendlyError?: UserFriend }`

**interface ConversionFlowManagerProps** `{ onComplete?: (jobId: string; filename: string) => void; onError?: (error: string) => void; showReport?: boolean; autoReset?: boolean; resetDelay?: num }`

**component ConversionFlowManager** (props: ConversionFlowManagerProps)

---

### `frontend/src/components/ConversionHistory/ConversionHistory.tsx`

**interface ConversionHistoryProps** `{ className?: string; maxItems?: number; onStartNewConversion?: () => void }`

**interface UsageStats** `{ tier: string; conversions_this_month: number; remaining: number; monthly_limit: number; should_upgrade: boolean; upgrade_message: string | null }`

**component ConversionHistory** (props: ConversionHistoryProps)

**hook useConversionHistory()**

---

### `frontend/src/components/ConversionHistory/ConversionHistoryItem.tsx`

**interface ConversionHistoryItemProps** `{ item: IConversionHistoryItem; isSelected: boolean; onToggle: (jobId: string) => void; onDelete: (jobId: string) => void; onDownload: (jobId: string; f }`

---

### `frontend/src/components/ConversionProgress/ConversionProgress.tsx`

**interface AgentStatus** `{ name: string; status: 'pending' | 'running' | 'completed' | 'failed'; progress: number; message?: string }`

**interface ExtendedConversionStatus** `{ agents?: AgentStatus[]; current_agent?: string }`

**interface ConversionProgressProps** `{ jobId: string | null; status?: string; progress?: number; message?: string; stage?: string | null }`

**component CheckmarkIcon**

**component PendingIcon**

**component SpinnerIcon**

**component ConversionProgress** (props: ConversionProgressProps)

---

### `frontend/src/components/ConversionReport/AssumptionsReport.tsx`

**interface AssumptionsReportProps** `{ assumptions: AssumptionsReportType; isExpanded: boolean; onToggle: () => void }`

**interface AssumptionCardProps** `{ assumption: AssumptionDetail }`

**component ImpactBadge**

**component ConfidenceIndicator**

**component AssumptionCard** (props: AssumptionCardProps)

**component ImpactDistribution**

**component CategoryBreakdown**

**component FilterControls**

**component AssumptionsReport** (props: AssumptionsReportProps)

---

### `frontend/src/components/ConversionReport/ConversionReport.tsx`

**interface ConversionReportProps** `{ conversionResult: InteractiveReport; jobStatus?: 'completed' | 'failed' | 'processing' }`

**component ConversionReport** (props: ConversionReportProps)

---

### `frontend/src/components/ConversionReport/ConversionReportContainer.tsx`

**interface ConversionReportContainerProps** `{ jobId: string; jobStatus?: 'completed' | 'failed' | 'processing' }`

**component ConversionReportContainer** (props: ConversionReportContainerProps)

---

### `frontend/src/components/ConversionReport/DeveloperLog.tsx`

**interface DeveloperLogProps** `{ log: DeveloperLogType; isExpanded: boolean; onToggle: () => void }`

**interface LogSectionProps** `{ title: string; logs: LogEntry[]; icon?: string }`

**interface PerformanceMetricsProps** `{ metrics: Record<string }`

**component LogLevelBadge**

**component LogEntryComponent**

**component LogSection** (props: LogSectionProps)

**component PerformanceMetrics** (props: PerformanceMetricsProps)

**component OptimizationSuggestions**

**component BenchmarkComparisons**

**component ExportButton**

**component DeveloperLog** (props: DeveloperLogProps)

---

### `frontend/src/components/ConversionReport/EnhancedConversionReport.tsx`

**interface EnhancedConversionReportProps** `{ reportData: InteractiveReport; jobStatus?: 'completed' | 'failed' | 'processing' }`

**interface NavigationItem** `{ id: string; title: string; icon: string; count?: number; hasErrors?: boolean }`

**component ExportControls**

**component QuickNavigation**

**component ReportMetadata**

**component GlobalSearch**

**component EnhancedConversionReport** (props: EnhancedConversionReportProps)

---

### `frontend/src/components/ConversionReport/FeatureAnalysis.tsx`

**interface FeatureAnalysisProps** `{ analysis: FeatureAnalysisType; isExpanded: boolean; onToggle: () => void }`

**interface FeatureCardProps** `{ feature: FeatureConversionDetail }`

**component CompatibilityScore**

**component StatusBadge**

**component FeatureCard** (props: FeatureCardProps)

**component SearchBar**

**component FilterDropdown**

**component CategoryBreakdown**

**component FeatureAnalysis** (props: FeatureAnalysisProps)

---

### `frontend/src/components/ConversionReport/ReportIssue.tsx`

**interface ReportIssueProps** `{ jobId: string; modName: string; version: string; conversionScore: number; failingCategories: string[] }`

**interface IssueReportPayload** `{ job_id: string; mod_name: string; version: string; conversion_score: number; failing_categories: string[]; description: string; severity: 'low' | 'med }`

**component ReportIssue** (props: ReportIssueProps)

---

### `frontend/src/components/ConversionReport/ReportSummary.tsx`

**interface ReportSummaryProps** `{ summary: SummaryReport }`

**interface CategoryBreakdownProps** `{ categories: SummaryReport['category_breakdown'] }`

**component CircularProgress**

**component StatCard**

**component DownloadButton**

**component QualityIndicator**

**component RecommendedActions**

**component CategoryBreakdown** (props: CategoryBreakdownProps)

**component PriorityOrder**

**component ReportSummary** (props: ReportSummaryProps)

---

### `frontend/src/components/ConversionUpload/ConversionOptions.tsx`

**interface ConversionOptionsProps** `{ smartAssumptions: boolean; onSmartAssumptionsChange: (checked: boolean) => void; includeDependencies: boolean; onIncludeDependenciesChange: (checked:  }`

**component ConversionOptions** (props: ConversionOptionsProps)

---

### `frontend/src/components/ConversionUpload/ConversionUpload.tsx`

**interface ConversionUploadProps** `{ onConversionStart?: (jobId: string) => void; onConversionComplete?: (jobId: string) => void }`

**component ConversionUpload** (props: ConversionUploadProps)

---

### `frontend/src/components/ConversionUpload/ConversionUploadEnhanced.tsx`

**interface ConversionUploadProps** `{ onConversionStart?: (jobId: string; filename: string) => void; onConversionComplete?: (jobId: string) => void; onConversionFailed?: (jobId: string; er }`

**component ConversionUploadEnhanced**

---

### `frontend/src/components/ConversionUpload/ConversionUploadReal.tsx`

**interface ConversionUploadProps** `{ onConversionStart?: (jobId: string) => void; onConversionComplete?: (jobId: string) => void }`

**interface ConversionStatus** `{ job_id: string; status: string; progress: number; message: string; error?: string | null }`

**interface ConversionResponse** `{ job_id: string; status: string; message: string }`

**interface UploadResponse** `{ file_id: string; original_filename: string; saved_filename: string; message: string }`

**component ConversionUploadReal**

---

### `frontend/src/components/Editor/AssetManager/AssetList.tsx`

**component AssetList**

---

### `frontend/src/components/Editor/AssetManager/AssetManager.tsx`

**component AssetManager**

---

### `frontend/src/components/Editor/AssetManager/AssetUpload.tsx`

**component AssetUpload**

---

### `frontend/src/components/Editor/BedrockDocsPanel/BedrockDocsPanel.tsx`

**interface BedrockDocItem** `{ id: string; title: string; description: string; category: | 'entity'; syntax?: string; example?: string; properties?: Record<string; related?: string[ }`

**interface BedrockDocsPanelProps** `{ className?: string; onInsertSnippet?: (snippet: string) => void }`

**component BedrockDocsPanel** (props: BedrockDocsPanelProps)

---

### `frontend/src/components/Editor/BlockList/BlockList.tsx`

**component BlockList**

---

### `frontend/src/components/Editor/PreviewWindow/PreviewWindow.tsx`

**component PreviewWindow**

---

### `frontend/src/components/Editor/PropertiesPanel/PropertiesPanel.tsx`

**component PropertiesPanel**

---

### `frontend/src/components/Editor/RecipeManager/RecipeEditorPanel.tsx`

**component RecipeEditorPanel**

---

### `frontend/src/components/Editor/RecipeManager/RecipeList.tsx`

**component RecipeList**

---

### `frontend/src/components/Editor/RecipeManager/RecipeManager.tsx`

**component RecipeManager**

---

### `frontend/src/components/ErrorBoundary/ErrorBoundary.tsx`

**interface Props** `{ children: ReactNode; fallback?: ReactNode; onRetry?: () => void; isRetryable?: boolean; retryMessage?: string }`

**interface State** `{ hasError: boolean; error: Error | null; errorInfo: ErrorInfo | null }`

**component WrappedComponent**

**hook useErrorHandler()**

---

### `frontend/src/components/ExportManager/ExportManager.tsx`

**interface ExportManagerProps** `{ conversionId: string; open: boolean; onClose: () => void }`

**interface ExportOptions** `{ format: 'mcaddon' | 'zip' | 'json'; includeTemplates: boolean; includeMetadata: boolean; compression: 'none' | 'standard' | 'maximum'; customFileName? }`

**component ExportManager** (props: ExportManagerProps)

---

### `frontend/src/components/MermaidDiagram/MermaidDiagram.tsx`

**interface MermaidDiagramProps** `{ chart: string; id?: string; className?: string }`

**component MermaidDiagram** (props: MermaidDiagramProps)

---

### `frontend/src/components/NotificationSystem/NotificationSystem.tsx`

**interface Notification** `{ id: string; type: 'success' | 'error' | 'warning' | 'info'; title: string; message?: string; duration?: number; persistent?: boolean; action?: {; labe }`

**interface NotificationContextType** `{ addNotification: (notification: Omit<Notification; removeNotification: (id: string) => void; clearAll: () => void }`

**interface NotificationProviderProps** `{ children: React.ReactNode; maxNotifications?: number; defaultDuration?: number }`

**interface NotificationContainerProps** `{ notifications: Notification[]; onRemove: (id: string) => void }`

**interface NotificationItemProps** `{ notification: Notification; onRemove: (id: string) => void }`

**component NotificationProvider** (props: NotificationProviderProps)

**component NotificationContainer** (props: NotificationContainerProps)

**component NotificationItem** (props: NotificationItemProps)

**component WrappedComponent**

**hook useNotifications()**

**hook useSuccessNotification()**

**hook useErrorNotification()**

**hook useWarningNotification()**

**hook useInfoNotification()**