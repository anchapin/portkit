# Core FastAPI for AI Engine API - align with backend version
fastapi==0.136.1
uvicorn[standard]==0.47.0
python-multipart==0.0.29

# AI Framework — LangChain/LangGraph stack (issue #1201 — legacy multi-agent framework removed)
# Use Python >= 3.11 for langchain/langgraph compatibility.
openai>=2.37.0,<3.0.0
langgraph>=1.2.0
langchain-core>=1.4.0
langchain-openai>=1.2.1,<2.0
langchain-ollama>=1.1.0
langgraph-checkpoint-sqlite>=3.1.0
sentence-transformers>=5.5.0,<6.0.0

# Vector Database — pin relaxed after legacy framework removal (#1439, unblocked by #1201)
chromadb>=1.5.9,<2.0

# Data Processing - align with backend version
numpy
python-dotenv~=1.2.2

# Java analysis - tree-sitter for Java 17+ support
# Note: javalang is still required by logic_translator.py
tree-sitter>=0.25.2
tree-sitter-java
javalang==0.13.0

# File processing - align with backend version
python-magic==0.4.27
Pillow>=12.2.0
pydub
jsonschema

# Database - used by learning/correction_store and knowledge/schema
sqlalchemy>=2.0.49

# HTTP Client - align with backend version
httpx==0.28.1
beautifulsoup4>=4.14.3 # For web scraping (BedrockDocsScraper)
aiofiles>=25.1.0     # For async file operations (potentially in scraper or other utils)
validators>=0.35.0   # For URL validation (BedrockDocsScraper)

# Web Search - use available version
duckduckgo-search>=8.1.1

# Search engine
rank_bm25>=0.2.2

# Redis for job management - align with backend version
redis>=7.4.0
async-timeout>=5.0.1

# Security overrides for transitive dependencies (CVE-2026-23949, Issue #1483)
jaraco.context>=6.1.2

# Configuration Management - align with backend version
pydantic~=2.13.4
pydantic-settings~=2.14.1

# Monitoring - align with backend version
prometheus_client>=0.25.0
structlog>=24.0.0

# Distributed Tracing - OpenTelemetry
# Note: chromadb provides opentelemetry-exporter-otlp-proto-grpc which conflicts
# with opentelemetry-exporter-jaeger-proto-grpc on protobuf versions.
# Using flexible versions and letting pip/legacy-resolver find a solution.
opentelemetry-api>=1.41.1
opentelemetry-sdk>=1.41.1
opentelemetry-exporter-otlp>=1.41.1
opentelemetry-instrumentation-fastapi>=0.62b1
opentelemetry-instrumentation-httpx>=0.62b1
opentelemetry-instrumentation-redis>=0.62b1
